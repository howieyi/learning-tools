{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build:iframe:ts": "lerna run --scope @howieyi/iframe build:tsc",
    "build:iframe:father": "lerna run --scope @howieyi/iframe build",
    "deploy": "lerna publish from-package --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
